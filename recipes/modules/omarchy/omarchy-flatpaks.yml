type: default-flatpaks
configurations:
  - notify: false
    scope: user
    repo:
      title: Flathub (user)
  - notify: false
    scope: system
    repo:
      title: "Flathub (system-wide)"
    install:
      # The proprietary / unpackaged GUI apps Omarchy ships by default
      - com.spotify.Client
      - md.obsidian.Obsidian
      - org.signal.Signal
      - org.localsend.localsend_app
      - us.zoom.Zoom
